/**
 * 전사 지사 목록.
 *
 * 유지보수 업무는 지사별로 독립 운영된다 — 오버홀 회차·보수계획·수량산출서가
 * 모두 지사 하나에 속하고, 화면은 지사를 먼저 고른 뒤 그 지사의 데이터만 본다.
 *
 * docgen(문서 자동생성)의 constants.ts에도 같은 목록이 있다. 그쪽은 별도
 * Supabase를 쓰는 독립 모듈이라 당장 합치지 않는다 — 지사가 바뀌면 두 곳을
 * 함께 고칠 것.
 */
export const BRANCHES = [
  "중앙지사", "강남지사", "파주지사", "삼송지사", "고양사업소", "화성지사",
  "동탄지사", "판교지사", "광교지사", "용인지사", "분당사업소", "평택지사",
  "광주전남지사", "양산지사", "대구지사", "세종지사", "김해사업소", "청주지사",
];

export function isBranch(v: unknown): v is string {
  return typeof v === "string" && BRANCHES.includes(v);
}
