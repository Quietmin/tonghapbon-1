# PlantSync Pro — 발전소 오버홀 공정관리 웹앱

발전소 정기 오버홀 기간 중 기계·전기·제어 분야의 공정 현황을 관리하는 웹앱입니다.
설계내역서 엑셀을 업로드하면 작업항목을 자동 추출하고, 물량 기준 공정률·지연 위험·입력 누락을 자동 산정하여 대시보드로 시각화합니다.

## 실행 방법

```bash
npm install
npm run dev      # http://localhost:5175
npm run build    # 배포용 정적 빌드 (dist/)
```

> 서버·DB 없이 브라우저에서 완결되는 프론트엔드 단독 앱입니다. 데이터는 브라우저 IndexedDB에 저장됩니다.

## 화면 구성 (PRD 대응)

| 화면 | 경로 | 설명 |
|---|---|---|
| 대시보드 | `/` | 전체/분야별/설비별 공정률, 경과일, 지연 위험, 알림, 보고서 |
| 업로드 분석 | `/upload` | 프로젝트·계약기간(시작일~준공일) 설정, **분야(자동/기계/전기/제어) 지정 업로드**, 여러 파일을 대기열에 누적(하나씩 드래그 가능) 후 일괄 추가, 파일별 삭제 / 전체 리셋 |
| 작업 관리 | `/tasks` | 작업 검색·필터·페이지네이션, 상태/진행률 |
| 공정표 | `/schedule` | 작업명 키워드 기반 계획일정 자동배치 + 간트차트(설비별 그룹·오늘 기준선·지연 강조·필터) |
| 실적 입력 | `/entry` | 일자별 완료 물량·작업내용·지연사유·분해 전/후 사진 입력, **날짜별 독립 저장 + 항목별 정비 이력 타임라인**(과거 기록 조회·재편집·삭제) |
| 보고서 | `/reports` | 일일보고서·경영진 요약 보고서 자동 생성, 인쇄/PDF 출력 |

우측 상단에서 **권한(관리자·담당자·조회자)** 을 전환하여 각 역할별 제한을 확인할 수 있습니다.

## 엑셀 자동분석 엔진 (`src/lib/excelParser.js`)

PRD 6장 구현 — 실제 한국식 설계내역서(64개 시트, 자간 공백 헤더, 원가/집계 시트 혼재)를 처리합니다.

- **컬럼 자동인식**: 명칭/품명/항목/대상설비, 규격, 수량, 단위, 비고, Tag No. (공백 무시 매칭)
- **제외 처리**: 표지·원가계산·집계·일위대가 등 요약 시트 제외, 소계/합계/헤더/노트/수식 행 제외
- **분야 분류**: 설비→분야 매핑 우선(GT/ST/HRSG/DH→기계 등), 키워드 폴백
- **설비 분류**: GT, ST, HRSG, DH, 발전기, 전기설비, 제어설비, 펌프/밸브, 배관, 비계
- **중복 제거**: 집계·산출 시트에 반복되는 동일 작업 dedupe
- **확인 필요 플래그**: 수량·단위·분야가 불명확한 항목 표시

로컬 검증: `node scripts/test-parse.mjs` (기초자료 엑셀로 추출 결과 확인)

## 공정률 산정 (`src/lib/progress.js`)

- 작업별 공정률 = 실제 완료수량 / 계획수량 × 100
- 분야별/설비별/전체 = 물량 가중(Σ완료 / Σ계획)
- 지연 위험 = 계획(선형) 대비 실적이 임계치 이상 뒤처진 미완료 작업

## 공정표 자동 생성 (`src/lib/schedule.js`)

PRD 8장 구현 — 계획일정 결정 우선순위:

1. **엑셀 `작업 예정일` 컬럼**(있으면 우선) — `2026-07-10~2026-07-15` 형태의 기간을 파싱해 그대로 계획일정으로 사용. `~` 범위·단일일자·`.`/`/` 구분자·엑셀 날짜 일련번호 지원. 헤더는 `작업예정일/예정일/작업일정/일정` 등으로 자동 인식.
2. **작업명 키워드 자동배치**(예정일이 없을 때) — 키워드(준비/분해/점검/검사/정비/조립/시험)를 기간 내 구간에 매핑, 전기·제어 점검류는 전체 병행 배치
- **계획 공정률**: 계획일정 기준 물량 가중 진척(PRD 8.4) — 대시보드의 선형 계획률보다 정밀
- **지연 판정**: 계획 대비 실적 15%p 이상 미달 시 지연 위험
- **준공 예정일 추정**: 현재 진행 속도 기준 선형 추정 → 경영진 보고서 반영

## 기술 스택

React 18 · Vite 5 · React Router 6 · Tailwind CSS 3 · SheetJS(xlsx) · idb-keyval(IndexedDB)

디자인 토큰은 Stitch "Lumina Overhaul Director"(Apple 스타일 라운드/글래스모피즘) 기준.
